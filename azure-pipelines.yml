
pool:
  vmImage: 'windows-2019'

steps:
- task: VSBuild@1
  inputs:
    solution: '**\*.sln'
    platform: 'X86'
    clean: true
    maximumCpuCount: true
    restoreNugetPackages: true
    createLogFile: true